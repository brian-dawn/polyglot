# nim example

# With nix

Open it:

    nix develop -c code .

Run it:

    nix develop -c nim r src/main.nim

# Without nix

Make sure choosenim is installed.

Install nimlsp:

    nimble install nimlsp
